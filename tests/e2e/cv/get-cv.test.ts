import { describe, it } from "vitest";

describe("GET /v1/users/:userId/cv", () => {
  it("returns CV data correctly", async () => {
    //
  });

  it("returns UNAUTHENTICATED without access token", async () => {
    //
  });
});
